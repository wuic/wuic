/**
 * Alert some text.
 *
 * @param something the text
 */
function saySomeThing(something) {
    alert(something);
}

/**
 * Text applied by default
 *
 * @type {string}
 */
var CONST = "This is the polling sample from WUIC";

saySomeThing(CONST);

